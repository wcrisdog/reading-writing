# 4.4 技术实现路径

## 4.4.1 技术架构总览

**智引文思**采用"轻量前端 + Serverless 后端 + AI 大模型"的现代化技术架构，确保快速迭代、低成本部署和高可用性。

```
┌─────────────────────────────────────────────────────────┐
│                     用户层（浏览器）                        │
│   HTML5 + CSS3 + Vanilla JavaScript（无框架依赖）         │
└───────────┬─────────────────────────────────────────────┘
            │
            ├─── localStorage（本地数据持久化）
            │
            ├─── 编辑器模块（实时统计、自动保存）
            │
            └─── AI Service 调用层
                    │
                    ↓
┌───────────────────────────────────────────────────────────┐
│           Vercel Edge Network（全球CDN加速）               │
│                 Serverless Functions                      │
│                  /api/qianwen.js                          │
└───────────────────┬───────────────────────────────────────┘
                    │
                    ├─── API Key 安全管理（环境变量）
                    ├─── 请求代理与流量控制
                    └─── 双模型降级策略
                            │
                            ↓
┌───────────────────────────────────────────────────────────┐
│            阿里云 DashScope API                           │
│     qwen-plus (主) ⇄ qwen-turbo (备用)                    │
│        超时重试 + 自动降级机制                             │
└───────────────────────────────────────────────────────────┘
```

**核心设计理念：**
- **轻量化**：零依赖框架，首屏加载 < 1s
- **安全性**：API Key 仅存于服务端，前端不暴露敏感信息
- **容错性**：双模型降级 + 本地灵感库兜底
- **可扩展性**：Serverless 架构支持自动扩容


---

## 4.4.2 前端技术方案

### 1. 技术选型
| 技术栈 | 版本/说明 | 选型理由 |
|--------|-----------|----------|
| **HTML5** | Semantic Markup | 清晰的 DOM 结构，便于 SEO 和无障碍访问 |
| **CSS3** | 响应式设计 + Grid/Flexbox | 支持多设备适配，无需 UI 框架 |
| **JavaScript** | ES6+ 原生实现 | 避免框架臃肿，减少打包体积 |
| **localStorage** | 5MB 容量 | 离线存储草稿和历史记录 |

### 2. 核心功能模块

#### （1）智能写作编辑器
- **实时统计**：字符数、字数、段落数动态计算
- **自动保存**：每 30 秒自动保存至 `localStorage`，避免数据丢失
- **手动操作**：保存、清空、导出 `.txt` 文件
- **卡顿检测**：监控用户输入间隔，超过 2 分钟无输入自动触发灵感提示

```javascript
// 核心逻辑示例（简化版）
function autoSave() {
    const content = mainEditor.value;
    localStorage.setItem('currentDraft', JSON.stringify({
        title: titleInput.value,
        text: content,
        type: currentType,
        timestamp: Date.now()
    }));
}

setInterval(autoSave, 30000); // 每 30 秒自动保存
```

#### （2）AI 服务调用层（`ai-service.js`）
- **统一接口**：封装所有 AI 功能的 API 调用
- **超时控制**：单次请求超时 25 秒，自动重试 1 次
- **错误处理**：网络异常时降级到本地提示库

**调用流程：**
```
用户点击"启动引导" 
  → 构建 Prompt（系统提示 + 用户输入）
  → 发送 POST /api/qianwen/ 
  → 返回 AI 反馈 
  → 渲染到右侧面板
```

#### （3）四大核心 AI 功能实现

**A. 启动引导（Launch Guidance）**
- **流程设计**：议论文/记叙文/学术论文各 5-6 步问答
- **AI 反馈节点**：第 3 步对用户回答进行分析与建议
- **最终产物**：生成包含素材、字数、要点的详细大纲

**B. 素材推荐（Material Recommendation）**
- **输入参数**：文章类型、主题、用户偏好历史
- **AI 生成**：名言警句、历史事例、场景描写等分类素材
- **本地降级**：AI 不可用时使用 `materialLibrary` 本地素材库

**C. 灵感提示（Inspiration Tips）**
- **触发机制**：
  - 手动点击按钮触发
  - 自动检测 2 分钟无输入自动触发
- **上下文感知**：AI 分析当前已写内容，给出针对性建议

**D. 逻辑修补（Logic Repair）**
- **AI 分析**：检测论证逻辑漏洞、叙事连贯性问题
- **引导式反馈**：不直接修改，而是提出引导性问题
- **迭代修正**：用户回答后再次生成优化建议


---

## 4.4.3 后端服务架构

### 1. Serverless 函数（`api/qianwen.js`）

**核心职责：**
- 接收前端请求，转发至阿里云 DashScope API
- 管理 `QIANWEN_API_KEY`（通过 Vercel 环境变量配置）
- 实现双模型降级：`qwen-plus` → `qwen-turbo`
- 请求超时控制与重试

**关键代码逻辑：**
```javascript
// 双模型降级策略
const attempts = [
    { model: 'qwen-plus', maxTokens: 1300, timeoutMs: 16000 },
    { model: 'qwen-turbo', maxTokens: 910, timeoutMs: 12000 }
];

for (let i = 0; i < attempts.length; i++) {
    try {
        response = await callQianwen(attempts[i]);
        if (response.ok) break; // 成功则跳出
    } catch (error) {
        if (i === attempts.length - 1) throw error; // 最后一次尝试失败
    }
}
```

### 2. API 安全设计

| 安全措施 | 具体实现 |
|----------|----------|
| **密钥保护** | API Key 仅存于 Vercel 环境变量，前端不可见 |
| **CORS 控制** | 限制 Origin 白名单（生产环境可配置） |
| **请求限流** | Vercel 内置流量控制，防止恶意攻击 |
| **超时保护** | 单次请求最长 30 秒（Vercel Serverless 限制） |

### 3. AI 模型配置

| 功能模块 | 模型选择 | Max Tokens | 温度参数 |
|---------|----------|------------|----------|
| 启动引导 | qwen-plus | 1000 | 0.7 |
| 详细大纲 | qwen-plus | 1300 | 0.7 |
| 素材推荐 | qwen-plus | 1000 | 0.7 |
| 灵感提示 | qwen-turbo | 450 | 0.7 |
| 逻辑修补 | qwen-plus | 800 | 0.7 |

**设计考量：**
- **灵感提示**使用 `qwen-turbo`（响应更快，成本更低）
- **大纲生成**使用 `qwen-plus`（输出质量更高，结构更完整）


---

## 4.4.4 数据存储方案

### 1. 浏览器本地存储（localStorage）

**存储内容：**
```javascript
{
    "currentDraft": {
        "title": "长期主义的力量",
        "text": "...",
        "type": "argumentative",
        "timestamp": 1709622000000
    },
    "history": [
        { "id": "draft_001", "title": "我的成长故事", "..." },
        { "id": "draft_002", "title": "教育改革的思考", "..." }
    ],
    "currentOutline": {
        "sections": [...],
        "materials": [...]
    },
    "userPreferences": {
        "targetWords": 800,
        "language": "zh",
        "autoSaveEnabled": true
    }
}
```

**优势：**
- ✅ 无需后端数据库，降低运营成本
- ✅ 数据完全归用户所有，隐私安全
- ✅ 离线可用，无需联网即可写作（AI 功能除外）

**限制：**
- ❌ 单个域名 5-10MB 容量
- ❌ 清除浏览器缓存会丢失数据（已提示用户导出）
- ❌ 无法跨设备同步（后续可接入云存储）

### 2. 未来扩展方案（V2.0）

| 功能 | 技术方案 | 预期收益 |
|------|----------|----------|
| **云端同步** | Vercel KV（Redis）/ Supabase | 跨设备访问，数据永久保存 |
| **协作写作** | WebSocket + CRDT 算法 | 多人实时协作编辑 |
| **数据分析** | 埋点 SDK（如 Umami/Plausible） | 了解用户使用习惯，优化功能 |


---

## 4.4.5 部署与运维

### 1. 部署方案

**选择 Vercel 的原因：**
- **一键部署**：GitHub 集成，推送代码自动构建
- **全球 CDN**：Edge Network 加速，国内外访问速度优化
- **Serverless**：按需计费，初期成本几乎为零
- **环境变量管理**：安全配置 `QIANWEN_API_KEY`

**部署流程：**
```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 本地开发
npm run dev  # 启动 Vercel Dev Server

# 3. 生产部署
vercel --prod

# 4. 配置环境变量
vercel env add QIANWEN_API_KEY
```

### 2. 性能优化

| 优化项 | 实现方式 | 效果 |
|--------|----------|------|
| **静态资源缓存** | `Cache-Control: max-age=31536000` | 减少重复加载 |
| **代码压缩** | Vercel 自动 minify | 减小 JS/CSS 体积 30% |
| **CDN 加速** | Vercel Edge Network 全球节点 | 国内访问延迟 < 200ms |
| **懒加载** | 大纲面板按需渲染 | 首屏加载时间缩短 40% |

### 3. 监控与日志

**日志策略：**
- **前端错误**：`window.onerror` 捕获异常，上报至控制台
- **API 调用**：记录请求时长、Token 消耗、错误码
- **性能监控**：Vercel Analytics（免费版）跟踪页面加载时间

**示例日志：**
```javascript
console.log('✅ 千问API响应成功', {
    type: 'detailed-outline',
    essayType: 'argumentative',
    tokens: 856,
    duration: '2.3s'
});
```


---

## 4.4.6 技术创新点

### 1. "引导式" AI 交互模式
**区别于传统 AI 写作工具（如 ChatGPT 直接生成）：**
- ❌ **传统模式**：用户输入 → AI 直接生成完整文章 → 学生丧失思考能力
- ✅ **本产品模式**：分步提问 → AI 启发引导 → 学生独立完成写作

**技术实现：**
- 设计 6 步渐进式问题库（`guidanceQuestions`）
- 第 3 步插入 AI 反馈节点，分析用户回答
- 最终生成"大纲框架"而非成品文章

### 2. 双模型降级 + 本地兜底
**三层容错机制：**
```
1️⃣ qwen-plus（高质量，16s 超时）
   ↓ 失败
2️⃣ qwen-turbo（快速响应，12s 超时）
   ↓ 仍失败
3️⃣ 本地提示库（离线可用）
```

**技术价值：**
- 保证 **99%+ 可用性**，即使 AI 服务异常也能正常使用
- 成本控制：灵感提示优先使用 `qwen-turbo`（价格仅 qwen-plus 的 1/3）

### 3. 智能卡顿检测
**算法逻辑：**
```javascript
let lastActivityTime = Date.now();

mainEditor.addEventListener('input', () => {
    lastActivityTime = Date.now();
});

setInterval(() => {
    const idleTime = Date.now() - lastActivityTime;
    if (idleTime > 120000 && mainEditor.value.length > 50) {
        triggerInspirationTip(); // 自动弹出灵感提示
    }
}, 30000); // 每 30 秒检查一次
```

**用户体验价值：**
- 无需主动求助，AI 主动介入
- 避免学生"卡壳"时放弃写作


---

## 4.4.7 可扩展性与安全保障

### 1. 技术扩展路径

| 阶段 | 功能扩展 | 技术方案 |
|------|----------|----------|
| **V1.0（当前）** | 核心写作功能 + AI 辅助 | Vercel + 通义千问 |
| **V1.5（3 个月）** | 云端同步、多设备登录 | Supabase Auth + KV 存储 |
| **V2.0（6 个月）** | 个性化学习路径、错题本 | 用户画像分析 + TensorFlow.js |
| **V3.0（1 年）** | 协作写作、教师批改功能 | WebSocket + 权限管理系统 |

### 2. 安全性保障

| 风险点 | 防护措施 |
|--------|----------|
| **API Key 泄露** | 仅存于 Vercel 环境变量，前端代码不包含 |
| **恶意请求攻击** | Vercel 内置 DDoS 防护 + 请求频率限制 |
| **用户数据泄露** | 采用 localStorage 本地存储，数据不上传服务器 |
| **内容版权风险** | AI 仅生成"引导性建议"，最终文章由学生独立完成 |

### 3. 成本控制策略

**通义千问 API 定价（2026年2月）：**
- `qwen-plus`：¥0.004/1k tokens（输入）+ ¥0.012/1k tokens（输出）
- `qwen-turbo`：¥0.001/1k tokens（输入）+ ¥0.003/1k tokens（输出）

**单次使用成本估算：**
```
启动引导（6 步）：~2000 tokens × ¥0.012 = ¥0.024
素材推荐：~1500 tokens × ¥0.012 = ¥0.018
灵感提示（3 次）：~1200 tokens × ¥0.003 = ¥0.004
逻辑修补：~1500 tokens × ¥0.012 = ¥0.018
─────────────────────────────────────────
单用户单次完整使用：约 ¥0.064（6.4 分）
```

**月度成本预测（1000 活跃用户）：**
- 假设每用户每月使用 4 次：1000 × 4 × ¥0.064 = **¥256**
- Vercel 免费额度：100GB 流量 + 100 小时函数运行时间
- **总成本（初期）：< ¥500/月**


---

## 4.4.8 技术实施时间表

| 阶段 | 时间 | 里程碑 | 技术任务 |
|------|------|--------|----------|
| **阶段一** | 已完成 | 核心功能上线 | ✅ 前端编辑器 + 四大 AI 功能 + Vercel 部署 |
| **阶段二** | 1 个月内 | 性能优化 | 🔨 代码分割、CDN 加速、错误监控 |
| **阶段三** | 2-3 个月 | 用户反馈迭代 | 📊 数据埋点、A/B 测试、Prompt 优化 |
| **阶段四** | 3-6 个月 | 产品增强 | 🚀 云端同步、移动端适配、支付系统 |


---

## 4.4.9 技术风险与应对

| 风险 | 可能性 | 影响 | 应对方案 |
|------|--------|------|----------|
| **通义千问 API 不可用** | 低 | 高 | 双模型降级 + 本地提示库兜底 |
| **Vercel 服务中断** | 极低 | 中 | 备用方案：Netlify/Cloudflare Pages |
| **成本超支** | 中 | 低 | 设置 Vercel 预算告警 + 请求限流 |
| **浏览器兼容性问题** | 低 | 中 | Polyfill + 降级方案（IE11 不支持） |
| **数据隐私争议** | 低 | 高 | 明确隐私政策：数据本地存储，不上传服务器 |


---

## 4.4.10 总结：技术方案的核心优势

| 维度 | 传统开发方案 | 本项目技术方案 |
|------|--------------|----------------|
| **开发周期** | 3-6 个月（需搭建后端数据库） | 1 个月（Serverless 架构） |
| **初期成本** | ￥5000-10000/月（服务器 + 数据库） | < ￥500/月（按用量计费） |
| **扩展性** | 需手动扩容服务器 | 自动扩容，无需运维 |
| **安全性** | 需自建鉴权系统 | Vercel 内置安全防护 |
| **AI 集成** | 需自研 Prompt 工程 | 直接调用通义千问 API |

**技术护城河：**
1. **"引导式" AI 交互模式**：专利布局空间（教育科技类）
2. **三层容错机制**：保证服务高可用性
3. **轻量化架构**：快速迭代，适应市场需求

---

**附：技术栈总览**
```yaml
前端技术:
  - 核心: HTML5 + CSS3 + Vanilla JavaScript (ES6+)
  - 存储: localStorage (5MB)
  - 编辑器: 原生 <textarea> + 实时统计

后端技术:
  - 平台: Vercel Serverless Functions (Node.js 18+)
  - API 代理: /api/qianwen.js
  - 环境变量: QIANWEN_API_KEY

AI 服务:
  - 提供商: 阿里云 DashScope
  - 模型: qwen-plus (主) / qwen-turbo (备)
  - 调用方式: RESTful API (HTTPS)

部署运维:
  - 主机: Vercel (全球 CDN)
  - 监控: Vercel Analytics
  - 日志: Console.log + 前端错误捕获

开发工具:
  - 包管理: npm
  - 版本控制: Git + GitHub
  - 本地开发: vercel dev
```

---

*注：本技术方案已在生产环境验证，代码开源于 GitHub（wcrisdog/reading-writing）。*
